import Box from "../Box";
import IconButton from "../IconButton";

/**
 * Share component
 * @returns
 */
const Share = () => {
  return (
    <Box>
      <IconButton icon="share" />
    </Box>
  );
};

export default Share;
